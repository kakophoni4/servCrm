DATABASE_URL=postgresql://crm:crm@localhost:5432/crm?schema=public
JWT_SECRET=dev-change-me
JWT_EXPIRES_IN=7d
PORT=3001
CORS_ORIGIN=http://localhost:3000
UPLOAD_DIR=./uploads
# AES-256-GCM: 64 hex chars, base64(32 bytes) или произвольная строка (sha256)
APP_ENC_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
# Публичный HTTPS URL API для Telegram setWebhook (без /api)
# Домен не обязателен: бот работает через getUpdates (polling).
BASE_URL=
TELEGRAM_BOT_TOKEN=
