# Справочник: категории ЗП мастера

> Placeholder. Заменить таблицей из Excel заказчика 1:1.
> Формула: `master_pct = category_by(work_sum).percent`

Правило подбора: первая категория, где `work_sum >= min_sum` и (`max_sum` is null или `work_sum <= max_sum`), иначе ближайшая сверху/снизу — **уточнить**.

| min_sum | max_sum | percent | note |
|---------|---------|---------|------|
| 0 | 3999 | 0.50 | микра / черновик |
| 4000 | 7999 | 0.45 | черновик |
| 8000 | 14999 | 0.40 | черновик |
| 15000 | null | 0.35 | черновик |

Настраивает директор (категории) / владелец (всё). Автопересчёт — фаза 2.
